---
title: "181_project"
author: "Aislinn Gilmour"
date: "2023-10-04"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

Install packages
```{r}
library(ggplot2)
library(dbplyr)
library(tidyverse)
library(sf)
```
Load in data
Country population data: https://theodora.com/wfb2003/rankings/population_2003_0.html
```{r}
rawdata <- read.csv("~/Documents/QBS_Fall_2023/Data_Wrangling/sars_2003_complete_dataset_clean.csv")
```

Data Exploration
```{r}
#no missing data
missing_counts <- colSums(is.na(rawdata))
missing_counts
#summary
summary(rawdata)

#change date to date type
rawdata$Date<-as.Date(rawdata$Date)
rawdata$Country<-as.factor(rawdata$Country)

#data ranges from march 17,2003 to july 7, 2003
summary(rawdata)

table(rawdata$Country)

```

library called tmap 
use ggmap 

GIS Stuff
Data from: https://international.ipums.org/international/gis.shtml
```{r}
world_map <- st_read("/Users/aislinngilmour/Documents/QBS_Fall_2023/Data_Wrangling/IPUMSI_world_release2020/world_countries_2020.shp")
```
```{r}
 #Create a map with country boundaries
ggplot() +
  geom_sf(data = world_map) +
  geom_sf(data = world_map, color = "black", fill = NA) +
  labs(title = "World Map with Country Borders")
```
```{r}
install.packages('tmap')
library(tmap)
```

```{r}

```
